#include <stdio.h>
#include <stdlib.h>

void main () {
    //Conseguiu fazer com a mente, apenas vendo o ex anterior
    const double PI = 3.1492138; //Colocamos assim em vez de colocar no #import
    double c, r = 5.0; //Já declaramos o valor de "r" na msm linha

    c = 7 * PI * r; //coloca o valor da soma
    printf("O valor dessa BOMBA É: %f", c); //Já mostra o valor
    system("pause");
}